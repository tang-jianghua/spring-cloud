server:
  port: 7000
spring:
  application:
    name: zipkin-server

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8000}/eureka/
  instance:
    prefer-ip-address: true
